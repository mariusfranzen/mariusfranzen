import React, { ReactElement } from 'react'

interface Props {
    
}

function Resume({}: Props): ReactElement {
    return (
        <div>
            Resume
        </div>
    )
}

export default Resume
